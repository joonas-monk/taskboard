---
phase: 09-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - ecosystem.config.js
  - deploy.sh
  - nginx/taskboard.conf
  - .env.example
  - .gitignore
autonomous: true
requirements:
  - DATA-02

must_haves:
  truths:
    - "next.config.ts enables standalone output with native module tracing for better-sqlite3 and Prisma"
    - "ecosystem.config.js runs server.js in fork mode with single instance and all required env vars"
    - "deploy.sh automates the full redeploy cycle: pull, install, generate, migrate, build, copy assets, restart"
    - "Nginx config template proxies HTTPS to localhost:3000 with 300s timeout for AI pipeline"
    - ".env.example documents all required environment variables without real secrets"
  artifacts:
    - path: "next.config.ts"
      provides: "Standalone build configuration with outputFileTracingIncludes"
      contains: "output.*standalone"
    - path: "ecosystem.config.js"
      provides: "PM2 process management configuration"
      contains: "exec_mode.*fork"
    - path: "deploy.sh"
      provides: "Automated redeploy script"
      contains: "prisma generate"
    - path: "nginx/taskboard.conf"
      provides: "Nginx reverse proxy template"
      contains: "proxy_pass.*127.0.0.1:3000"
    - path: ".env.example"
      provides: "Environment variable documentation"
      contains: "ANTHROPIC_API_KEY"
  key_links:
    - from: "ecosystem.config.js"
      to: ".next/standalone/server.js"
      via: "script field in PM2 config"
      pattern: "script.*standalone/server\\.js"
    - from: "ecosystem.config.js"
      to: ".env.production"
      via: "dotenv require at top of config"
      pattern: "dotenv.*env\\.production"
    - from: "deploy.sh"
      to: "ecosystem.config.js"
      via: "pm2 restart/start at end of deploy"
      pattern: "pm2.*(restart|start)"
---

<objective>
Create all deployment configuration files needed to run TaskBoard on the Hostinger VPS with PM2, Nginx, and Let's Encrypt.

Purpose: Prepare the codebase with production-ready configs so the VPS setup (Plan 02) can reference committed, version-controlled files rather than ad-hoc commands.
Output: next.config.ts (standalone), ecosystem.config.js (PM2), deploy.sh (redeploy script), nginx/taskboard.conf (reverse proxy template), .env.example (variable docs)
</objective>

<execution_context>
@/Users/harimaa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/harimaa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-deployment/09-RESEARCH.md

@next.config.ts
@package.json
@src/lib/prisma.ts
@src/actions/ai.ts
@.gitignore
</context>

<interfaces>
<!-- Key patterns the executor needs from existing code -->

From src/actions/ai.ts — worker spawn env vars that must be available in production:
```typescript
env: {
  DATABASE_URL: process.env.DATABASE_URL!,
  ANTHROPIC_API_KEY: process.env.ANTHROPIC_API_KEY!,
  PIPELINE_MODEL: process.env.PIPELINE_MODEL ?? 'claude-3-5-haiku-20241022',
  NODE_ENV: process.env.NODE_ENV ?? 'production',
  PATH: process.env.PATH,
  HOME: process.env.HOME,
},
```

From src/lib/prisma.ts — database URL fallback:
```typescript
const connectionString = process.env.DATABASE_URL ?? 'file:/var/data/taskboard/taskboard.db'
```

From src/actions/ai.ts — worker path resolution (must work with PM2 cwd):
```typescript
const workerPath = path.resolve(process.cwd(), 'src/workers/pipeline-worker.ts')
const tsxPath = path.resolve(process.cwd(), 'node_modules/.bin/tsx')
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Next.js standalone config + PM2 ecosystem + env template</name>
  <files>next.config.ts, ecosystem.config.js, .env.example</files>
  <action>
1. **Update next.config.ts** — Enable standalone output mode with outputFileTracingIncludes for native modules:
   ```typescript
   output: 'standalone',
   outputFileTracingIncludes: {
     '/*': [
       './node_modules/better-sqlite3/**/*',
       './node_modules/@prisma/adapter-better-sqlite3/**/*',
       './src/generated/prisma/**/*',
     ],
   },
   ```
   Keep the existing NextConfig type import. Do NOT add any other config options.

2. **Create ecosystem.config.js** — PM2 config at project root. CRITICAL constraints from research:
   - `exec_mode: 'fork'` (NOT cluster — SQLite cannot handle multi-process writes)
   - `instances: 1` (single instance only)
   - `HOSTNAME: '127.0.0.1'` (only listen on loopback — Nginx proxies externally)
   - Load `.env.production` via `require('dotenv').config({ path: '.env.production' })` at top (more reliable than PM2's env_file option)
   - Pass all env vars the worker needs: DATABASE_URL, ANTHROPIC_API_KEY, PIPELINE_MODEL, NODE_ENV, PORT, HOSTNAME, HOME, PATH
   - Set `cwd` to project root (NOT .next/standalone) because pipeline worker resolves tsx from process.cwd()
   - Log files at `/var/log/taskboard/out.log` and `/var/log/taskboard/err.log`
   - Use the exact code from research section "Verified: ecosystem.config.js for This Project"

3. **Create .env.example** — Document all required env vars with placeholder values and comments:
   - DATABASE_URL=file:/var/data/taskboard/tasks.db
   - ANTHROPIC_API_KEY=sk-ant-api03-... (with comment: get from Anthropic Console)
   - PIPELINE_MODEL=claude-3-5-haiku-20241022 (with comment: optional, defaults to this)
   - NODE_ENV=production
   - PORT=3000
   - HOSTNAME=127.0.0.1
   Do NOT include real secrets. This file IS committed to git.
  </action>
  <verify>
    <automated>node -e "const c = require('./next.config.ts'); console.log('standalone check skipped — ts file')" 2>/dev/null; test -f ecosystem.config.js && node -e "const c = require('./ecosystem.config.js'); console.assert(c.apps[0].exec_mode === 'fork', 'must be fork mode'); console.assert(c.apps[0].instances === 1, 'must be 1 instance'); console.log('ecosystem.config.js valid')" && test -f .env.example && echo ".env.example exists"</automated>
  </verify>
  <done>next.config.ts has standalone output + outputFileTracingIncludes, ecosystem.config.js has fork mode with all env vars, .env.example documents all required variables</done>
</task>

<task type="auto">
  <name>Task 2: Deploy script + Nginx config template + gitignore update</name>
  <files>deploy.sh, nginx/taskboard.conf, .gitignore</files>
  <action>
1. **Create deploy.sh** at project root — Automated redeploy script for the VPS. Must be idempotent (safe to run repeatedly). Use the exact script from research "Verified: Deploy Script" with these steps:
   - `set -e` for fail-fast
   - `cd /home/deploy/taskboard`
   - git pull origin main
   - `npm ci --production=false` (devDeps needed for tsx + prisma CLI)
   - `npx prisma generate`
   - `DATABASE_URL="file:/var/data/taskboard/tasks.db" npx prisma migrate deploy`
   - `npm run build`
   - Copy public and static into standalone: `cp -r public .next/standalone/` and `cp -r .next/static .next/standalone/.next/`
   - `pm2 restart taskboard || pm2 start ecosystem.config.js`
   - Print status with `pm2 list`
   Make the script executable comment at top: `#!/bin/bash`

2. **Create nginx/taskboard.conf** — Nginx reverse proxy template. Create the `nginx/` directory. Use the verified Nginx config from research with:
   - Port 80 server block redirecting to HTTPS
   - Port 443 server block with SSL placeholders (Certbot fills these in)
   - `server_name` set to `yourdomain.com www.yourdomain.com` (user replaces with actual domain)
   - `proxy_pass http://127.0.0.1:3000`
   - `proxy_read_timeout 300s` (AI pipeline can take 30-120 seconds)
   - `client_max_body_size 10M`
   - All proxy headers: Host, X-Real-IP, X-Forwarded-For, X-Forwarded-Proto, Upgrade, Connection
   - Add a comment at top: "Copy to /etc/nginx/sites-available/taskboard and replace yourdomain.com"

3. **Update .gitignore** — Ensure deployment files are handled correctly:
   - `.env*` is already ignored (covers .env.production) — verify this line exists
   - `.env.example` must NOT be ignored — add `!.env.example` exception line after `.env*`
   - `ecosystem.config.js` should be committed (NOT ignored)
   - `deploy.sh` should be committed (NOT ignored)
   - Do NOT modify any other gitignore rules
  </action>
  <verify>
    <automated>test -f deploy.sh && test -f nginx/taskboard.conf && grep -q "!.env.example" .gitignore && echo "All deployment files created and gitignore updated"</automated>
  </verify>
  <done>deploy.sh is executable bash script with 6-step redeploy, nginx/taskboard.conf has full reverse proxy config with 300s timeout, .gitignore excludes .env.production but allows .env.example</done>
</task>

</tasks>

<verification>
- `next.config.ts` contains `output: 'standalone'` and `outputFileTracingIncludes` with better-sqlite3
- `ecosystem.config.js` loads with Node.js, uses fork mode, single instance, passes all env vars
- `.env.example` exists with all 6 env vars documented (no real secrets)
- `deploy.sh` exists with prisma generate + migrate deploy + build + pm2 restart steps
- `nginx/taskboard.conf` exists with proxy_pass to 127.0.0.1:3000 and 300s timeout
- `.gitignore` has `!.env.example` exception
</verification>

<success_criteria>
All 6 deployment configuration files exist in the repository, committed to git. The codebase is ready for VPS deployment — no code changes needed on the server, only infrastructure setup (Plan 02).
</success_criteria>

<output>
After completion, create `.planning/phases/09-deployment/09-01-SUMMARY.md`
</output>
